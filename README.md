# role-engine-users
This repository will contains all the backend


# Requirements

This service is expecting a DB defined on `application.properties`

# Run the service

Use next command to run the service:

```bash
mvn spring-boot:run
```

# Tests

To be sure that any change break any old functionality run:

```bash
mvn test
```